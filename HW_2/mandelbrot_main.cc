#include <cstdio>
#include "mandelbrot_header.h"

int main(){
    double a = .01, b = .01;

    if (i == maxIter)
      printf("(%.2f, %.2f) is in the Mandelbrot set.\n", a,b);
    else 
      printf("(%.2f, %.2f) is not the Mandelbrot set.\n", a,b);

    return 0;
}
